// Консольный TUI (ТЗ 6, UI). Дизайн-референс: docs/tui.
//
// Собран на Win32 Console API без внешних зависимостей: один статический
// бинарь под Windows 7. Палитра Nocturne задаётся ремапом 16-цветной таблицы
// консоли (SetConsoleScreenBufferInfoEx работает с Vista).
#pragma once

namespace certmig {

// Запускает интерактивный интерфейс. Возвращает код возврата процесса.
int RunTui();

// Отрисовать ключевые экраны в stdout как текст (без интерактивного ввода) -
// для проверки вёрстки. Цвета игнорируются.
int RunTuiDump();

}  // namespace certmig
